---
name: project_ibkr_crypto_derivatives_track
description: Queued second track — IBKR unlocks shortable crypto derivatives + futures-basis/options-skew data (the non-OHLCV edge)
metadata: 
  node_type: memory
  type: project
  originSessionId: 8763b088-8df0-42fa-b565-1f51a17b1900
---

2026-07-12: Explored whether a cross-asset hedge (FX + crypto/stocks/options) is feasible and whether IBKR provides more crypto data than binance.us/freqtrade. Conclusion, **queued as a second track behind finishing the FX factor stack** (user chose "c then b": FX value/PPP + carry+momentum+value blend first, then brainstorm this).

**Key findings:**
- **IBKR is single-venue** for FX + crypto + stocks + options with cross-margin — no two-broker parity problem.
- **It relieves the shorting wall** that blocked the whole crypto RV program: short crypto exposure via **CME BTC/ETH futures** (+ micros) and **options on spot-BTC ETFs (IBIT)** / shorting the ETFs. The prior "can't short crypto on US spot" wall ([[project_xsectional_reversion]], [[project_funding_signal_weak_pass]]) does NOT bind an IBKR account using derivatives.
- **Data:** IBKR is NOT more spot OHLCV (binance.us+ccxt wins on coins + free deep history). IBKR's value is *qualitatively new* data: **crypto futures term structure (basis/roll = shortable carry, the tradeable analog of the funding signal from [[project_funding_signal_weak_pass]] Study #7)** and **options implied-vol surface (skew/term = crypto vol-risk-premium)** — exactly the non-OHLCV data Study #7 concluded is the only thing that moves the edge.

**Why:** Spot-FX-majors tops out ~8–10% ([[project_fx_return_bar]]); the crypto-derivatives complex is a higher-effort but potentially higher-edge avenue that only became reachable with IBKR.

**How to apply:** When the FX stack is done, brainstorm a **crypto futures-basis / options-skew strategy** (shortable, real carry/vol-premium signal, optionally beta-hedged vs FX/equities). Caveats to design around: CME/OPRA market data are **paid subscriptions**; IBKR **historical derivatives depth is thin/rate-limited** → need a separate backtest history source (Deribit free-ish IV history — not US-tradeable but usable for research — or CME/vendor). Verify IBKR specifics (coin list, IBIT options, subscriptions) against current docs — they change. This is a significant new modeling surface (Greeks/roll/margin) + a DataView/backtest extension, not a quick add.

**Refinement 1 — the ~20 Zero Hash coins are the LIQUID-MAJOR universe (2026-07-12).** That's the OPPOSITE of where the spot edge lived (high-vol illiquid alts, ZEC — [[project_universe_selection_no_lever]]): on liquid majors the gbb/NN stack made ~0 trades / −34% forced. So the spot factor/NN algos are a POOR fit for IBKR crypto. The GOOD fit is [[project_xsectional_momentum_regime]] — long-only cross-sectional momentum + BTC>SMA regime, which already showed diversified multi-year returns on liquid majors, and reuses the same rank machinery as the new FX `momentum` strategy. Constraint: only **BTC/ETH** have US-listed shortable derivatives (CME futures / IBIT options); the other ~18 coins are **spot, long-only** at IBKR → market-neutral is blocked on the alts; long-only cross-sectional only, with the shortable/hedged sleeve limited to BTC/ETH. So the crypto track shapes up as: long-only x-sectional momentum on the ~20 + a BTC/ETH derivatives (basis/vol) sleeve.

**Refinement 2 — idle cash earns interest at IBKR (2026-07-12).** Competitive tiered interest on settled USD cash above a threshold (~4%+, verify current rate/threshold — moves with rates). Answers the original "what to do in a bear market instead of sitting in cash" question: a regime strategy that de-risks to cash isn't giving up risk-free carry, so flat/risk-off is a legitimate low-cost state. Cleaner/safer than crypto staking/lending (counterparty + regulatory risk — same reason binance.us earn products were unpromising). Framework refinement (minor, do when a strategy actually holds cash): accrue **risk-free on the un-deployed cash fraction** in the backtest — parallel to the deferred carry-accrual note in the live-execution spec. Currently the backtest assumes fully-deployed books.

**ASSESSED 2026-07-17 → PORT NOT WORTHWHILE; standalone funding/basis edge GATED NEGATIVE. Interest-on-cash is the only real benefit (needs no port).** Full go/no-go run this session:
- **Spot-strategy port = pointless.** IBKR spot crypto (~20 coins via Paxos/Zero Hash, 0.12-0.18% comm) is SAME-OR-WORSE data than binance.us+ccxt (fewer coins, no free deep history). The freqtrade edge lived in illiquid ALTS (ZEC); IBKR's liquid-major universe is exactly where that stack made ~0 trades / lost. Nothing to gain.
- **New since 2026-07-12:** IBKR added (Feb 2026) **Coinbase Derivatives nano BTC/ETH futures incl. perpetual-style** (CFTC DCM, 24/7, shortable, margin, funding-rate mechanism, 0.01 BTC / 0.10 ETH per contract). This unlocks the shorting + funding signal that US spot blocked — the ONE signal that survived the crypto program (funding-reversion, Study #7). IBIT-vs-CME carry wedge (~2.58pp ann.) is real but retail costs eat it (source's own caveat) — deprioritized.
- **Interest on idle USD cash (verified 2026-07-17):** 3.13% (NAV>$100k, bal>$10k) up to ~4.33% IBKR Pro; first $10k earns 0. Account-level, accrues regardless of strategy → makes a risk-off "sit in cash" state ~free. This is the only genuine reason to custody at IBKR; **no port needed to get it.**
- **DATA-FEASIBILITY SPIKE = PASS.** BTC/ETH funding history obtainable FREE at multi-era depth: **data.binance.vision static dumps reachable from US IP** (fundingRate 8h, 2020-01→present; markPriceKlines + spot klines for basis). Live-append via **OKX ccxt** (~90d rolling; Binance-global/Bybit geo-blocked from US) — freqtrade `refresh_funding.py` already does this Binance-history→OKX-live stitch. Toolchain reused: freqtrade `.venv` (ccxt 4.5.50). (No BTC/ETH feathers pre-existed — old universe was alts.)
- **SIGNAL-CHECK = FAIL (the deciding result).** Causal 8h perp backtest (long ret = price_return − funding), 2020-26, cost 0/5/10bp, distant-era splits. **Funding-reversion is REAL but cost-dominated:** gross Sharpe positive EVERY era (0.72/1.22/0.51/0.98) → genuine phenomenon, but edge (~0.73 gross full) < realistic perp cost — net 5bp flips 2023-24 and 2025-26 negative (−0.26/−0.02), dead by 10bp. Slowing to daily recovers 2023-24 (+0.33) but 2025-26 still −0.23. **Cross-sectional BTC-vs-ETH (the market-neutral Study-#7 analog) is DEAD** (net −1.2 to −4.1 every era; gross was turnover-churn that evaporates to 0.52 when slowed). Basis-carry redundant (corr 0.54, same mechanism, equally fragile). Same verdict pattern as [[project_fx_intraday_reversion_assessment]]: cost-dominated, only slow edges survive; confirms majors are efficient and the funding edge only lived in illiquid alts.
- **Reopen triggers (not permanently dead):** (1) **breadth** — 2 shortable assets is the core failure; if IBKR/Coinbase list nano perps on SOL/XRP/etc., the cross-sectional funding trade (needs breadth) becomes testable — highest-leverage, monitor. (2) confirmed **sub-2bp** real nano-perp all-in cost could let the slowed funding-timing sleeve clear most eras. (3) funding-as-OVERLAY (risk-off gate on long-only majors momentum), not standalone P&L — matches where crypto wins actually were (structural overlays, not edge predictors). Scripts: scratchpad `crypto_funding_signalcheck.py` + `_refine.py`. **Don't re-run the standalone funding/basis backtest — closed.**
