.venv/
data_cache/
__pycache__/
*.egg-info/
.pytest_cache/
.obsidian/
runs/
# Runtime forward-record artifacts — local records, not versioned.
# Back these up deliberately if you want the history (Constitution: Execution & Data Safety).
track.log
nav.csv
snapshot.log
launchd.err
*_positions.csv
health_status.txt
health.log
.superpowers/

# Machine-local Claude Code settings. .claude/skills/ and .specify/ ARE tracked,
# so the Spec Kit workflow works on any clone.
.claude/settings.local.json
